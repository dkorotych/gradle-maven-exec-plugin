language: groovy
sudo: false
before_cache:
  - rm -f $HOME/.gradle/caches/modules-2/modules-2.lock
cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/
matrix:
  include:
  - jdk: openjdk7
    env: ONLY_CHECK='true'
  - jdk: oraclejdk7
    env: ONLY_CHECK='true'
jdk:
  - oraclejdk8
before_script:
  - chmod +x gradlew
  - chmod +x travis.sh
script: ./travis.sh
